/// @description Inserir descrição aqui
// Você pode escrever seu código neste editor
if (place_meeting(x, y, Bloco_obj)) {
    activated = true; // Ativa a área quando o bloco está na posição
} else {
    activated = false;
}